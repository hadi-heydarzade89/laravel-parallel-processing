This is a test project to implement parallel processing
<h1>Parallel processing by curl</h1>
<p>using curl</p>
``php artisan parallel:curl sendRequestByCurl``
<p>using http guzzle package</p>
``php artisan parallel:curl sendByGuzzle``
